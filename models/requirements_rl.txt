# Reinforcement Learning Dependencies for LLM Ruff Fixer
# Install with: pip install -r requirements_rl.txt

# Core ML dependencies
torch>=2.0.0
numpy>=1.21.0

# Optional: For better performance on Apple Silicon
# torch>=2.0.0+metal  # For M1/M2 Macs

# Data handling
dataclasses-json>=0.5.0  # For JSON serialization of dataclasses

# Existing dependencies (should already be installed)
llama-cpp-python>=0.2.0
